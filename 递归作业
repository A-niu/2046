#include<stdio.h>
#include<stdlib.h>

int Steps(int n)
{
    
	if (1 == n)
		return 1;
	else if (2 == n)
		return 2;
	else
		return Steps(n - 1) + Steps(n - 2);
}
int main()
{
    
	int n;
	while (scanf("%d", &n) != EOF)
	{
    
		printf("台阶数为%d时，有%d种走法\n",n,Steps(n));
	}
	return 0;
}
